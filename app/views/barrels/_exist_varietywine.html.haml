.row
  .col-md-12
    .form-group
      = f.label :varietywine_id
      = f.select :varietywine_id,  Varietywine.all.map{|x| [x.name, x.id]}, {}, class: 'form-control'
      = f.error_messages_for(:varietywine_id)

  .actions.margin-small-top
    = f.submit nil, class: 'btn btn-primary'
